ai:
  model:
    # 本地覆盖配置，填入有效模型名和密钥后启用 AI 能力。
    type: CLOUD_MODEL
    modelName: ''
    temperature: 0.2
    maxTokens: 1024
    timeoutSeconds: 60
    maxRetries: 2
    cloud:
      baseUrl: https://api.openai.com/v1
      apiKey: ''
      organization: ''
